// State
const state = {
    auth: false
}

// Getters
const getters = {
    is_auth() {
        return state.auth;
    }
}

// Actions
const actions = {}

// Mutations
const mutations = {}

export default {
    state,
    getters,
    actions,
    mutations
}